import React from 'react';
import EmailForm from '../components/emailForm';

const Home = () => {
    return (
        <div>
            <h1>Welcome to the Email Subscription</h1>
            <EmailForm />
        </div>
    );
};

export default Home;